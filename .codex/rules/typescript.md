# TypeScript Rules

Codex mirror of `.claude/rules/typescript.md`.

Use `.claude/rules/typescript.md` as the canonical source for TypeScript style and typing rules until a Codex-specific variant is needed.
