# Plugin Rules

Codex mirror of `.claude/rules/plugins.md`.

Use `.claude/rules/plugins.md` as the canonical source for plugin architecture and extension rules until a Codex-specific variant is needed.
