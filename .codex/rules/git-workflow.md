# Git Workflow Rules

Codex mirror of `.claude/rules/git-workflow.md`.

Use `.claude/rules/git-workflow.md` as the canonical source for commit, push, and PR workflow rules until a Codex-specific variant is needed.
