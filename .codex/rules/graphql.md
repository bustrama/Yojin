# GraphQL Rules

Codex mirror of `.claude/rules/graphql.md`.

Use `.claude/rules/graphql.md` as the canonical source for schema, resolver, and GraphQL contract rules until a Codex-specific variant is needed.
