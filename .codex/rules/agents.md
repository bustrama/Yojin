# Agent Rules

Codex mirror of `.claude/rules/agents.md`.

Use `.claude/rules/agents.md` as the canonical source for multi-agent architecture, tool registration, and orchestration rules until a Codex-specific variant is needed.
