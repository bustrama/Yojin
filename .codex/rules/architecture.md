# Architecture Rules

Codex mirror of `.claude/rules/architecture.md`.

Use `.claude/rules/architecture.md` as the canonical source for module boundaries and system architecture rules until a Codex-specific variant is needed.
