# Worktree + Jira

Codex mirror of `.claude/commands/worktree-jira.md`.

Use `.claude/commands/worktree-jira.md` as the canonical Jira-driven worktree workflow until this command needs Codex-specific steps.
