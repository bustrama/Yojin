# Create Tool

Codex mirror of `.claude/commands/create-tool.md`.

Use `.claude/commands/create-tool.md` as the canonical workflow for adding a new tool until this command needs Codex-specific steps.
