# Create Channel

Codex mirror of `.claude/commands/create-channel.md`.

Use `.claude/commands/create-channel.md` as the canonical workflow for adding a new channel until this command needs Codex-specific steps.
