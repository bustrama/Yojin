# Create Guard

Codex mirror of `.claude/commands/create-guard.md`.

Use `.claude/commands/create-guard.md` as the canonical workflow for adding a new guard until this command needs Codex-specific steps.
