# Regression Dog

Codex mirror of `.claude/commands/regression-dog.md`.

Use `.claude/commands/regression-dog.md` as the canonical regression-check workflow until this command needs Codex-specific steps.
